# This file serves as a template for the required environment variables.
# Copy this file to .env and fill in the actual values.
# The .env file is ignored by Git and should not be committed.

# --- PostgreSQL Database Configuration ---
# Used by Alembic and SQLAlchemy for migrations and backend logic.
DATABASE_URL="postgresql://user:password@host:port/dbname"

# --- Frontend Configuration (for Cloudflare Pages) ---
# These variables are used by the build.sh script to generate the frontend config.
# They should be set in the Cloudflare Pages project settings.
ZITADEL_ISSUER="https://your-instance.zitadel.cloud"
ZITADEL_CLIENT_ID="your_oidc_client_id"
N8N_API_URL="https://your-n8n-webhook-url.com/webhook/chat"

# --- Backend Services (for n8n Workflow) ---
# These keys are needed within the n8n workflow itself.
# While not used directly by the Python code, they are part of the overall project setup.
OPENAI_API_KEY="sk-your_openai_api_key"
HUBSPOT_API_KEY="your_hubspot_api_key"
